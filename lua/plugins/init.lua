-- Here we have plugins which do not need config...
return {
    -- has to be installed also in tmux.
    -- --> the same keys enable navigation between tmux
    -- panes and nvim buffers... ;)
    "christoomey/vim-tmux-navigator"
}
